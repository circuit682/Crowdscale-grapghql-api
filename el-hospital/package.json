{
  "name": "el-hospital",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ el-hospital",
    "create-local": "graph create --node http://localhost:8020/ el-hospital",
    "remove-local": "graph remove --node http://localhost:8020/ el-hospital",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 el-hospital",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.82.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
